ACTIVATE_ENV = "source workflow/envs/venv/bin/activate"
PYTHON = "python"

rule all:
    input:
        script="workflow/scripts/all.py"
    output:
        directory("plots/")
    shell:
        "mkdir {output} && {ACTIVATE_ENV} && {PYTHON} {input.script}"
